const homeAction = {
 /* async initView({state, getters, commit, dispatch}) {
    await commit('GetLayerInfos');
  },*/
   initView({state, getters, commit, dispatch}) {

     commit('sss','kkkk');
  },

};
export default homeAction;
